# 设计: [功能名称]

> **关联提案**: [proposal.md](./proposal.md)
> **关联用例**: [usecases.md](./usecases.md)

---

## 背景

[当前系统相关部分是什么状态？这次改动的上下文。]

---

## 目标 / 非目标

**目标 (Goals)**:
- [这次要达成什么]
- [解决什么问题]

**非目标 (Non-Goals)** — 明确不做的事:
- [不做X，因为...]
- [不做Y，留给后续版本]

---

## 方案设计

### 数据模型

```sql
-- 新增/修改的表
CREATE TABLE [table_name] (
    id TEXT PRIMARY KEY,
    ...
);
```

### API设计

```
POST /api/[endpoint]
  请求: { ... }
  响应: { ... }

GET /api/[endpoint]
  响应: { ... }
```

### 架构图（如需要）

```
[用户操作] → [Android客户端] → [后端API] → [AI服务]
                  │                  │
                  ▼                  ▼
             [本地SQLite]      [云端SQLite]
```

---

## 决策记录

### D1: [决策标题]

**决定**: [选择了什么]

**备选方案**:
| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| A: [方案名] | [优点] | [缺点] | ← 选择 |
| B: [方案名] | [优点] | [缺点] | 否决，因为... |

**理由**: [一句话说明为什么选A]

### D2: [下一个决策]
...

---

## 风险与权衡

| 类型 | 描述 | 应对 |
|------|------|------|
| [风险] | [可能出什么问题] | [怎么缓解] |
| [权衡] | [为了X牺牲了Y] | [可接受，因为...] |

---

## AI/安全特别设计（如涉及）

### Prompt变更
```
[新增/修改的 system prompt 片段]
```

### 新增Tool定义
```json
{
  "name": "[tool_name]",
  "description": "[二狗什么时候用、做什么]",
  "parameters": { ... }
}
```

### 安全防护
- [针对这个功能的特定安全措施]
- [输入sanitize规则]
- [权限控制]
